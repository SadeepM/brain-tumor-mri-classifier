# brain-tumor-mri-classifier
A deep learning project for classifying brain tumors from MRI scans using a custom MiniVGG-inspired CNN in PyTorch. Features 3 convolutional blocks with batch normalization, dropout regularization, and learning rate scheduling, trained on the Kaggle Brain Tumor MRI Dataset.
